#include "HardCodedData.h"
#include "FileHandling.h"


int main(int argc, char* argv[])
{
	HANDLE hfile = NULL;
	/*if (openFile(&hfile, fName, READ))
	{
 		return(1);
	}
	
	if (WriteToFile(hfile, fName, strlen(fName)))
	{
		closeFile(&hfile);
		return(1);
	}
	if (ReadFromFile(hfile, strTST, 20))
	{
		closeFile(&hfile);
		return(1);
	} 
	
	if (closeFile(&hfile))
	{
		return(1);
	}
	else
		return(0);
	*/
}
//HANDLE *hfile, char *fileName, DWORD accessMode, DWORD shareMode, DWORD creationDisposition, DWORD flagsAndAttributes